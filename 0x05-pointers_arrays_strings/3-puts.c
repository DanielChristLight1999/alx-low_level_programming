#include <stdio.h>
#include <string.h>

/**
 * _puts - This function prints a string to stdout
 *
 * @str: the string to be printed
 *
 * Return: Always void
 */

void _puts(char *str)
{
	puts(str);
}

#include <stdio.h>
#include <string.h>

/**
 * _strlen - This function returns the length of a string
 *
 * @s: string to be checked
 *
 * Return: The length of @n
 */
int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}

#include <stdio.h>
#include <string.h>

/**
 * _puts_recursion - prints a string and a new line
 *
 * @s: The string to be printed
 *
 * Return: Always void
 */

void _puts_recursion(char *s)
{
	puts(s);
}

#include <stdio.h>

/**
 * main - Entry point of this program
 *
 * @argc: Number of inputs in command
 * @argv: array of string of commands
 *
 * Return: Always (0) Success
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
